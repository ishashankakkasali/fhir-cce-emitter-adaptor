# Production profile — WARN root logging, more retries, longer shutdown
spring:
  lifecycle:
    timeout-per-shutdown-phase: 45s

emitter:
  openhim:
    retry:
      max-attempts: 5
      backoff-ms: 3000

logging:
  level:
    root: WARN
    org.openphc.cce: INFO
    org.springframework.web: WARN
    ca.uhn.fhir: WARN

management:
  endpoint:
    health:
      show-details: never
